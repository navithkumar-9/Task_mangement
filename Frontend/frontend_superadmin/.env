# Backend API URL (used in production builds)
# For local dev, leave empty or comment out — Vite proxy handles it
# For production, set to your hosted backend URL e.g. https://api.yourdomain.com/api
VITE_API_BASE_URL=
